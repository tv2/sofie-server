// import * as mongodb from 'mongodb'

/**
 * This is a migration template.
 * The 'up' is the actual migration and most be implemented.
 * The 'down' method is for rolling back the migration - it can be omitted.
 * Both methods accepts two parameters: 'db' and 'client'.
 * 'db' is the MongoDB Db class. 'client' is the MongoDB MongoClient class.
 *
 * To write the migration simply write a MongoDB Query: https://www.mongodb.com/docs/manual/query-api/
 */

// export async function up(db: mongodb.Db, client: mongodb.MongoClient): Promise<void> {
//   // TODO: Below is an example of the 'up' implementation
//   await db.collection('executedRundowns').updateOne({ name: 'NYHEDERNE-TEST.SOFIE.RKLI' }, { $set: { name: 'Hello World' }})
// }

// export async function down(db: mongodb.Db, client: mongodb.MongoClient): Promise<void> {
//   // TODO: Below is an example of the 'down' implementation
//   await db.collection('executedRundowns').updateOne({ name: 'Hello World' }, { $set: { name: 'NYHEDERNE-TEST.SOFIE.RKLI' }})
// }
